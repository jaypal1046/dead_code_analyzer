/// Result of pattern matching for class definitions
class ClassMatchResult {
  const ClassMatchResult({required this.className, required this.classType});

  final String className;
  final String classType;
}
